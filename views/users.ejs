<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Apartment Maintenance System App</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous" />
</head>

<body>
    <div class="container">
        <nav></nav>

        <main class="row">
            <h1>Users</h1>
            <div class="col-8">
                <form action="/users/update" method="POST">
                    <div>
                        <input name="user_id" type="hidden" class="form-control" value="<%= user.user_id %>" />
                    </div>
                    <div>
                        <label class="form-label">user_id:
                            <input required="true" name="user_id" type="number" class="form-control"
                                value="<%= user.user_id %>" /></label>
                    </div>
                    <div>
                        <label class="form-label">name:
                            <input required="true" name="name" type="text" class="form-control"
                                value="<%= user.name %>" /></label>
                    </div>
                    <div>
                        <label class="form-label">apt_number:
                            <input required="true" name="apt_number" type="number" class="form-control"
                                value="<%= user.apt_number %>" /></label>
                    </div>
                    <div>
                        <label class="form-label">email:
                            <input required="true" name="email" type="text" class="form-control"
                                value="<%= user.email %>" /></label>
                    </div>

                    <button type="submit" class="btn btn-primary">Update</button>
                </form>
            </div>
            <!-- /.col-8 -->

        </main>

        <footer>
            <div class="row">
                <div class="col-4">About</div>
                <div class="col-4">Made By Stephane</div>
            </div>
        </footer>
    </div>
</body>

</html>